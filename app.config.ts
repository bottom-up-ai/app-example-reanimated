import { ExpoConfig } from 'expo/config';

const config: ExpoConfig = {
	"name": "app-example-reanimated",
	"slug": "app-example-reanimated",
	"version": "1.0.2",
	"orientation": "portrait",
	"scheme": "app.example.reanimated",
	"userInterfaceStyle": "automatic",
	"assetBundlePatterns": ["**/*"],
	"newArchEnabled": true,
};

export default config;